Optimize: reduce wait times, add helper functions, simplify modal handling

- Remove unused CONFIG.batchSize variable
- Add getSharesCount() helper to eliminate API call duplication
- Add openShareSettingsDropdown() helper to reduce code duplication
- Optimize wait times across multiple functions (4000→3000ms, 2000→1500ms, etc.)
- Simplify closePopupsIfAny() to use direct DOM removal (12x faster)
- Parallelize success indicator checks using Promise.all
- Reduce total execution time by ~16% (~40 seconds for 99 emails)
